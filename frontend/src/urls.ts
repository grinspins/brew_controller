export const HOST = "localhost:8000";
export const BASE_URL = `http://${HOST}`;

export const STATE_URL = `ws://${HOST}/state`;
export const PROGRAM_URL = `${BASE_URL}/program`;
export const SET_STATE_URL = `${BASE_URL}/set_state`;
